# 第一阶段验证记录 · 2026-09-10

目标：EasyCon 170a，固定基线 `1aed001c0e2d3a32d211c39bec26546741626bd6`。

- 原始基线 GUI Release 编译成功；增强版整套解决方案 Release 编译成功。
- 解决方案回归 744 项全部通过：Core/Script 663、LSP 66、WinInput 11、FRLG UI 4。
- 其中新增 19 项 OCR 回归和 4 项 UI 回归。实际界面由 Avalonia Headless + Skia 创建、鼠标反向拖动选区、执行读取，并生成窗口图片供检查。包括坐标换算、区域保存与还原、不同分辨率拒绝载入、刷新同尺寸快照保留选区。
- OCR 回归覆盖公开日版与英文截图、720/1080/2160 高度、手动区域、越界与空区域、无画面、白屏、截断和缺位、旧模型名调用保持原引擎分流。另有真实数字片段合成的 `00895`、`00000` 和溢出 `99999` 契约检查；合成检查不计为额外实拍样本。
- 独立 ECS 通过本增强 170a CLI 与真实 `1.6.4-a+9c86137` format。原包两份主脚本通过真实 1.6.4-a format，原包 39 项 `Tools/check_*.py` 全部通过。
- 33 份固定资源通过 SHA-256 核对。

## 连续截图回放

以下是同一张静态截图的重复执行，用于检查稳定性和耗时，不是 500 个独立画面，也不代表实机准确率。

| 截图 | 期望结果 | 连续正确次数 | 首次耗时 | 后续中位耗时 |
| --- | --- | --- | --- | --- |
| 上游日版 1920×1080 | `45345` | 500/500 | 104.3 ms | 8.75 ms |
| 上游英文 1280×720 | `60895` | 500/500 | 83.1 ms | 6.53 ms |
| 用户历史日版预览截图 702×403 | `02104` | 500/500 | 77.9 ms | 7.56 ms |

每个进程从首次调用前到 500 次完成的私有内存净增加约 8.8–12.6 MiB，包含 JIT、图像库和模板缓存初始化，不据此认定不存在内存泄漏。没有用静态回放代替采集卡、设备自动变暗或数小时实机长跑。

用户历史截图仅在本地测试，未加入公开仓库或发布包。公开测试样本及模板来源见 `FRLG-OCR-SOURCES.md`。

## 待实机确认

1. 在用户当前采集卡的训练家卡片画面手动框选五位数字，确认默认/手动区域与实际分辨率一致。
2. 用独立 ECS 检查跨时刻三读确认、失败重试，以及主机变暗前后的拒绝结果是否符合预期。
3. 名称、性格、等级、能力值与文字双后端属于后续阶段，当前不声称已实现。
