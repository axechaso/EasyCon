# FRLG OCR 验证记录 · 2026-09-10

## r2 日文场景版

- 用户已确认 r1 TID 实机读取没有问题。本轮保持 TID 的模板、阈值、完整五位及前导零规则，并通过原有 19 项 OCR 回归。
- 全解决方案 Release 检查：764 项通过、0 项失败（Core/Script 682、LSP 66、WinInput 11、FRLG UI 5）。另有 3 个原有外部模型服务测试标记为 Explicit/NotExecuted，未执行。
- FRLG 专项共 38 项，包括原有 19 项和新增 19 项日文回归；界面共 5 项。格式检查 `dotnet format ... --severity error` 通过。日文场景测试与原 TID 独立 ECS 均通过增强 170a CLI format。
- 53 份固定资源与模型通过 SHA-256 核对，包括 1025 个日文名称及 OCR 别名、25 项性格、Paddle 模型及字表、Tesseract 日文模型和授权文本。
- 公开日版第一页：フシギダネ / 等级 6 / のうてんき，以及 デオキシス / 等级 30 / しんちょう 均正确。第二页读取 `70 / 119 / 21 / 119 / 23 / 107`，顺序为最大 HP、攻击、防御、特攻、特防、速度。
- 检查了 720/2160 高度的能力值缩放、目标集合命中与排除、未知候选拒绝、哈克龙/快龙不混用、25 项性格匹配、缺模型失败诊断。由实拍数字片段构成的 `0/70` 仍返回最大 HP `70`，该合成图只验证处理契约，不计为额外实拍样本。
- 野生等级已接入数字模板路径，公开英文战斗图的 `28` 通过；没有日文野生战斗实拍样本。野生等级默认区域是两位数示例，一位或三位数须按实际画面重新框选。
- Avalonia Headless + Skia 创建实际窗口，验证手动选区、保存/还原候选、复制调用内容、日文名称及性格读取，生成 `artifacts/frlg-jpn-window.png` 并目视检查。未将该界面测试当作原生采集卡或 Switch 长跑验收。
- 模型在 CPU 上运行，首次加载及每次文字识别比数字识别慢；不要把截图回放时间直接当作自动流程的固定延迟。模型缓存不替代设备变暗、画面切换及长时间运行的实机检查。
- 两张静态截图各连续执行 5 次均正确。本机名称首次约 4.93 秒，后续中位约 2.85 秒；性格首次约 9.67 秒，后续中位约 9.25 秒。样本性格需要三个阈值，CPU 推理较慢；这是当前性能限制，不适合按毫秒级固定耗时安排文字读取。内存包含模型/JIT初始化，不据此宣称长跑无泄漏。

当前范围为所列日文识别场景和独立 ECS 接口，未修改正式乱数、Seed、帧轴或按键流程。全量词典不代表已经对全部物种做了实拍验证。

## r1 TID 阶段历史记录

目标：EasyCon 170a，固定基线 `1aed001c0e2d3a32d211c39bec26546741626bd6`。

- 原始基线 GUI Release 编译成功；增强版整套解决方案 Release 编译成功。
- 解决方案回归 744 项全部通过：Core/Script 663、LSP 66、WinInput 11、FRLG UI 4。
- 其中新增 19 项 OCR 回归和 4 项 UI 回归。实际界面由 Avalonia Headless + Skia 创建、鼠标反向拖动选区、执行读取，并生成窗口图片供检查。包括坐标换算、区域保存与还原、不同分辨率拒绝载入、刷新同尺寸快照保留选区。
- OCR 回归覆盖公开日版与英文截图、720/1080/2160 高度、手动区域、越界与空区域、无画面、白屏、截断和缺位、旧模型名调用保持原引擎分流。另有真实数字片段合成的 `00895`、`00000` 和溢出 `99999` 契约检查；合成检查不计为额外实拍样本。
- 独立 ECS 通过本增强 170a CLI 与真实 `1.6.4-a+9c86137` format。原包两份主脚本通过真实 1.6.4-a format，原包 39 项 `Tools/check_*.py` 全部通过。
- 33 份固定资源通过 SHA-256 核对。

## 连续截图回放

以下是同一张静态截图的重复执行，用于检查稳定性和耗时，不是 500 个独立画面，也不代表实机准确率。

| 截图 | 期望结果 | 连续正确次数 | 首次耗时 | 后续中位耗时 |
| --- | --- | --- | --- | --- |
| 上游日版 1920×1080 | `45345` | 500/500 | 104.3 ms | 8.75 ms |
| 上游英文 1280×720 | `60895` | 500/500 | 83.1 ms | 6.53 ms |
| 用户历史日版预览截图 702×403 | `02104` | 500/500 | 77.9 ms | 7.56 ms |

每个进程从首次调用前到 500 次完成的私有内存净增加约 8.8–12.6 MiB，包含 JIT、图像库和模板缓存初始化，不据此认定不存在内存泄漏。没有用静态回放代替采集卡、设备自动变暗或数小时实机长跑。

用户历史截图仅在本地测试，未加入公开仓库或发布包。公开测试样本及模板来源见 `FRLG-OCR-SOURCES.md`。

## r1 当时的待确认项（历史）

1. 在用户当前采集卡的训练家卡片画面手动框选五位数字，确认默认/手动区域与实际分辨率一致。
2. 用独立 ECS 检查跨时刻三读确认、失败重试，以及主机变暗前后的拒绝结果是否符合预期。
3. r1 当时未接名称、性格、等级、能力值和文字双后端；这些场景现已在上述 r2 中接入。
